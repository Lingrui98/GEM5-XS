# Phase B-rev F1 verdict

## Verdict

F1 is implemented, but the first required smoke invalidates F1 as currently
defined.

The active task must STOP after Phase B-rev Step 2.

Do not run:

- Phase B-rev Step 3 `mcf_17135`
- Phase C 180-run sweep
- Phase D validation
- Phase E report generation

until glr chooses the next design direction.

## Why STOP

The v3 prompt says to stop if `gcc_pp_O2` shows TAGE mispredict rate lower than
MBTB miss rate, because that contradicts the expected F1 signal.

That is exactly what the current evidence shows:

- MBTB F1 miss rate: `0.231488`
- TAGE F1 mispredict rate: `0.116286`
- TAGE - MBTB: `-0.115202`
- `sway.reallocCount = 0`

Per-phase CSV evidence is consistent with the whole-run stats:

- `new_utility_mbtb` mean: `0.211704`
- `new_utility_tage` mean: `0.117407`
- `new_utility_tage - new_utility_mbtb` mean: `-0.094297`
- best positive phase gap: `0.018740`
- phases where TAGE > MBTB: `2 / 49`

## Classification

This is **not** a controller-wiring failure:

- `new_utility_*` columns are present and non-zero in
  `sway_phase_a_diag.csv`.
- The decision predicate reads the F1 values.
- The controller consistently produces `hold/not_valid` because the F1 gap is
  negative in 47 of 49 phases.

This is **not** a hysteresis-only issue:

- The best positive gap is `0.018740`.
- Most phases have negative gap.
- Running a 0.05 threshold diagnostic would not answer the v3 STOP condition,
  because the unexpected ordering is MBTB > TAGE.

The most likely issue is F1 stat comparability:

- MBTB `predMiss/(predMiss+predHit)` appears to measure a broader frontend
  lookup/miss surface than TAGE's conditional `updateMispred/updateFilteredEntries`
  denominator.
- The current F1 formula therefore ranks MBTB as higher pressure than TAGE on
  `gcc_pp_O2`, even though the rev expected the opposite.

## Recommendation for glr

Choose one:

1. Revise F1 to use a MBTB denominator aligned to TAGE-eligible conditional
   branch pressure, then rerun Step 2.
2. Abandon F1 and move to F2 ATD marginal MPKI.
3. Reopen P3' / Q-U4 if the data suggests TAGE-only-donee cannot produce a
   robust conserved-substrate transfer policy.

No SPEC v2 or DESIGN_DECISIONS file was modified.
